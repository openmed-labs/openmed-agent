(function () { const STORAGE_KEY = "openmed-theme"; function getStoredTheme() { try { return localStorage.getItem(STORAGE_KEY); } catch { return null; } } function setStoredTheme(theme) { try { localStorage.setItem(STORAGE_KEY, theme); } catch { } } function getSystemTheme() { return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light"; } function resolveTheme() { return getStoredTheme() || getSystemTheme(); } function applySiteTheme(theme) { document.documentElement.setAttribute("data-theme", theme); document.querySelectorAll("[data-theme-toggle]").forEach((button) => { const nextAction = theme === "dark" ? "Switch to light theme" : "Switch to dark theme"; button.setAttribute("aria-pressed", theme === "dark" ? "true" : "false"); button.setAttribute("aria-label", nextAction); button.dataset.theme = theme; button.removeAttribute("title"); }); } function initCopyButtons() { document.querySelectorAll("[data-copy-text]").forEach((button) => { if (button.dataset.copyBound === "true") { return; } button.dataset.copyBound = "true"; const idleLabel = button.getAttribute("data-copy-label") || "Copy command"; button.setAttribute("aria-label", idleLabel); button.title = idleLabel; button.addEventListener("click", async () => { const text = button.getAttribute("data-copy-text") || ""; const label = button.querySelector(".om-copy-btn__label"); try { await navigator.clipboard.writeText(text); button.dataset.copyState = "copied"; button.setAttribute("aria-label", "Copied to clipboard"); button.title = "Copied to clipboard"; if (label) { label.textContent = "Copied to clipboard"; } window.setTimeout(() => { button.dataset.copyState = ""; button.setAttribute("aria-label", idleLabel); button.title = idleLabel; if (label) { label.textContent = idleLabel; } }, 1400); } catch { button.dataset.copyState = ""; button.setAttribute("aria-label", idleLabel); button.title = idleLabel; } }); }); } function initThemeToggle() { document.querySelectorAll("[data-theme-toggle]").forEach((button) => { if (button.dataset.themeBound === "true") { return; } button.dataset.themeBound = "true"; button.addEventListener("click", () => { const nextTheme = resolveTheme() === "dark" ? "light" : "dark"; setStoredTheme(nextTheme); applySiteTheme(nextTheme); }); }); } function boot() { applySiteTheme(resolveTheme()); initThemeToggle(); initCopyButtons(); } if (document.readyState === "loading") { document.addEventListener("DOMContentLoaded", boot, { once: true }); } else { boot(); } })();